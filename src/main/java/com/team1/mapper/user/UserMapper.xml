<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.team1.mapper.user.UserMapper">
	<insert id="insert">
	INSERT INTO tb_user (id, pw, email, location, nickname, introduce, profileurl)
	VALUES (#{id}, #{pw}, #{email}, #{location}, #{nickname}, #{introduce}, #{profileurl})
	</insert>

	<select id="select" resultType="com.team1.domain.user.UserVO">
	SELECT id, pw, email, location, signupday, nickname<!-- location이 아니라 address여서 수정함 -->
	FROM tb_user
	WHERE nickname = #{nickname} <!-- 댓글 작업에서 id로 가져오면 작동이 안돼서 nickname으로 바꿈 -->
	</select>
	
	<update id="update">
	UPDATE tb_user
	SET pw = #{pw},
		email = #{email},
		location = #{location},
		nickName = #{nickName},
		introduce = #{introduce}
	WHERE
		id = #{id}
	</update>
	
	<delete id="delete">
	DELETE FROM tb_user
	WHERE id = #{id}
	</delete>
	
	<select id="login" resultType="com.team1.domain.user.UserVO">
	SELECT nickname, pw
	FROM tb_user
	WHERE nickname = #{nickname}
	AND pw = #{pw}
	</select>
	
	<select id="selectByNickName" resultType="com.team1.domain.user.UserVO">
	SELECT id, pw, email, location, signupday, nickname, introduce
	FROM tb_user
	WHERE nickname = #{nickname}
	</select>
</mapper>